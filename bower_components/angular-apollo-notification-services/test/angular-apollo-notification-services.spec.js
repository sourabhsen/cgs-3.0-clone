describe('angular-apollo-notification-services', function() {
  beforeEach(function() {
    // Setup task before running each spec
  });

  afterEach(function() {
    // Clean up task after each spec
  });

  it('should have tests', function() {
    expect(true).toBe(true);
  });
});
